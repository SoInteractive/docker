sudo: required
language: python
services:
  - docker
install:
  - pip install molecule==1.25.0
  # - pip install required driver (e.g. docker, python-vagrant, shade)
script:
  - molecule test

    #notifications:
    #  webhooks:
    #    url: 
